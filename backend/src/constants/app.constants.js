const CATEGORIES = ['Marketing', 'Technical', 'General'];

const PRIORITY = ['Low', 'Medium', 'High']; // not used in Q1 but kept for consistency

const SUBMISSION_STATUS = ['Draft', 'Published']; // optional

module.exports = {
  CATEGORIES,
  PRIORITY,
  SUBMISSION_STATUS,
};
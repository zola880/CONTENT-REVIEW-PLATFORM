const CATEGORIES = ['Marketing', 'Technical', 'General'];

const PRIORITY = ['Low', 'Medium', 'High']; // not used in Q1 but kept for consistency

const SUBMISSION_STATUS = ['Draft', 'Published']; // optional

module.exports = {
  CATEGORIES,
  PRIORITY,
  SUBMISSION_STATUS,
};
// This file defines constant values used throughout the content review platform backend. It includes predefined categories for content classification, priority levels for submissions (though not currently used), and submission statuses. These constants help maintain consistency across the application and make it easier to manage and update these values in a single location when needed.